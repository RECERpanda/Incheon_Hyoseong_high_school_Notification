# 📅 인천효성고 알림이 위젯 사용 및 자동 실행 가이드

이 프로그램은 파이썬(Python)으로 제작된 바탕화면 위젯입니다. 처음 다운로드하여 사용하는 분들은 아래 순서대로 초기 설정을 한 번만 진행해 주세요.

## 1단계: 필수 프로그램 설치 (파이썬)
컴퓨터에 파이썬이 설치되어 있지 않다면 먼저 설치해야 코드가 실행됩니다.
1. [파이썬 공식 홈페이지](https://www.python.org/downloads/)에 접속하여 최신 버전의 Python을 다운로드합니다. ('Download Python install manager' 버튼 하단에 위치한 'Or get the standalone installer for Python 3.14.5' 와 같은 문장에서 밑줄 쳐져 있는 부분을 클릭하여 최신 버전을 다운로드합니다.)
2. 다운로드한 설치 파일을 실행합니다.
3. **중요:** 설치 창의 맨 아래에 있는 **`Add Python 3.x to PATH` 체크박스를 반드시 선택**한 뒤, `Install Now`를 눌러 설치를 완료합니다. (이 체크박스를 누락하면 이후 과정에서 오류가 발생합니다.)

## 2단계: 필수 라이브러리 설치
위젯이 인터넷을 통해 급식과 시간표 데이터를 가져오기 위해 필요한 부품(`requests`)을 설치하는 과정입니다.
1. 키보드에서 `Windows 로고 키 + R`을 눌러 '실행' 창을 엽니다.
2. `cmd`라고 입력하고 엔터를 눌러 검은색 명령 프롬프트 창을 띄웁니다.
3. 아래 명령어를 그대로 복사하여 붙여넣고 엔터를 누릅니다.
   ```bash
   pip install requests
   ```
4. 설치가 완료되었다는 문구(`Successfully installed...`)가 나타나면 창을 닫아주세요.

## 3단계: 파일 설치
<img width="1917" height="668" alt="image" src="https://github.com/user-attachments/assets/92b77a10-c435-48a0-8853-d6671a05090c" />
<img width="1918" height="733" alt="image" src="https://github.com/user-attachments/assets/431d566f-7c04-4711-b8c6-9d811b4fc61c" />




1. 위 사진대로 클릭하여 '인천효성고 알림이.pyw' 파일을 다운로드 해주세요.

## 4단계: 컴퓨터 부팅 시 자동 실행 설정
매번 프로그램을 직접 켤 필요 없이, 컴퓨터를 켤 때마다 자동으로 위젯이 나타나도록 설정해 봅시다.
1. 다운로드한 `인천효성고 알림이.pyw` 파일을 마우스 우클릭하여 **바로 가기 만들기**를 선택한 후 바로 가기 파일을 '잘라내기' 합니다.
2. 키보드에서 `Windows 로고 키 + R`을 눌러 다시 '실행' 창을 엽니다.
3. `shell:startup`을 입력하고 엔터를 누르면 '시작 프로그램' 폴더가 열립니다.
4. 1번에서 잘라낸 바로 가기 파일을 이 시작 프로그램 폴더 안으로 붙여넣습니다.

---

### 💡 완료 및 실행 팁
모든 설정이 끝났습니다! 방금 옮긴 바로 가기 파일을 더블클릭하여 위젯을 실행해 보세요. 
학년과 반을 마우스로 선택하면 같은 폴더에 설정 파일이 자동 생성되어 마지막 값을 기억하며, 내일 컴퓨터를 켤 때부터는 자동으로 화면에 나타납니다.
